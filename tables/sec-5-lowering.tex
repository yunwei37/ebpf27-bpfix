\begin{table}[t]
  \centering
  \caption{The four \empiricalset rejections that print \texttt{R invalid mem access 'scalar'} yet are compiler-lowering artifacts. Each is a real upstream commit whose fix changes how the program is compiled. The same message also prints for 24 source bugs, which a message-only tool cannot tell apart.}
  \label{tab:lowering}
  \small
  \begin{tabular}{@{}l p{4.6cm}@{}}
    \toprule
    Case (upstream commit) & Build-level fix \\
    \midrule
    bcc \texttt{42c00adb}    & add \texttt{-O2}; the optimized frontend drops the bad load \\
    cilium \texttt{4d36cac2} & keep packet pointers 64-bit, blocking an ALU32 copy \\
    cilium \texttt{50c319d0} & force the tuple onto the stack before the load \\
    SO 53136145              & rederive the UDP pointer in each checked branch \\
    \bottomrule
  \end{tabular}
\end{table}
