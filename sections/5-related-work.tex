%%%%%%%%%%%%Outline%%%%%%%%%%%%%%%%%%
%%%
% message<three lines bear on \sys, each differing in the input it consumes or the output it produces>
% three lines bear on \sys, from the eBPF domain outward, each differing in input consumed or output produced
%%%
% message<eBPF tooling decides safety or sidesteps the verifier, but none diagnoses a rejection from its log>
% prior eBPF tooling decides safety or sidesteps the verifier; explaining a rejection is left to the developer
% empirical studies document the cost: rejections are hard to read and force rewriting working code
% offline re-provers (PREVAIL) and verifier audits decide or check safety, never reading a rejection log
% others sidestep the verifier: replace it, optimize the bytecode, or isolate at runtime
% none replays the log to locate the fix site and explain the loss; attribution is best-effort
%%%
% message<diagnosis tools read facts the checker owns or reword at the source level; \sys recovers below-source state from a foreign log>
% tools that diagnose a rejection exist outside eBPF, but read facts the checker built
% type-error diagnosis localizes from the checker's own constraints; surveys note raw messages are unhelpful
% Pretty Verifier maps the eBPF error to a source line from source, debug info, and the log
% these read owned facts or reword at the source level; none reconstructs the abstract-state lifecycle
% \sys recovers that state from the verifier log the kernel produced, below the source where lowering artifacts arise
%%%
% message<localization and proof-explanation techniques each need an input \sys lacks; \sys works from one log>
% techniques that pinpoint or explain a failure predate eBPF, but each needs an input \sys lacks
% spectrum needs a test suite, slicing needs a dependence graph, unsat-core needs solver artifacts
% \sys works from a single rejection log, locating the fix site and explaining the rejection
% the verifier simulates over abstract state and emits no such artifact
%%%%%%%%%%%%Outline%%%%%%%%%%%%%%%%%%

\section{Related Work}\label{sec:related-work}

\noindent Three lines of work bear on \sys, listed from the eBPF domain outward, and each differs from it in the input it consumes or the output it produces.

\noindent\textbf{eBPF verification and safety.} Prior eBPF tooling decides whether a program is safe or sidesteps the verifier, and explaining why a rejected program failed is left to the developer. Empirical studies document the cost: verifier rejections are hard to read and push developers to rewrite working code to pass the verifier~\cite{deokar2024empirical, gbadamosi2024ebpf}. Offline re-provers such as PREVAIL establish safety over their own abstract domain without reading a rejection log~\cite{gershuni2019simple}, and verifier audits check the soundness of its range and \texttt{tnum} operators or find logic bugs that admit unsafe programs~\cite{vishwanathan2023verifying, vishwanathan2022sound, sun2024validating}. Others sidestep the verifier altogether, replacing it with safe Rust or program synthesis~\cite{jia2025rex, zheng2024kgent}, optimizing the bytecode before it loads~\cite{xu2021synthesizing, mao2024merlin}, or isolating programs at runtime~\cite{lu2024moat, zhang2024hive, lim2023unleashing, lim2024safebpf, dwivedi2024fast}. None replays the verifier's own rejection log to locate the fix site and explain why the safety fact was lost; attributing that loss to a layer remains a best-effort prototype (\S\ref{sec:design}).

\noindent\textbf{Error diagnosis for compilers and checkers.} Tools that diagnose a rejection exist outside eBPF, but they read facts the checker built during its own check. Type-error diagnosis localizes the source responsible for an ill-typed program from the constraints or facts the type checker itself produced~\cite{pavlinovic2014finding, zhang2014toward, lerner2007searching, seidel2017learning}, and surveys document why raw compiler messages are unhelpful~\cite{becker2019compiler}. For eBPF, Pretty Verifier maps the verifier's error to a C source line and rewords it from the source, the object file's debug information, and the verifier output~\cite{rizza2025design}. These tools read facts the checker owns, or reword its message at the source level, and none reconstructs the abstract-state lifecycle a rejection log records. \sys recovers that state, such as register types, scalar ranges, and pointer provenance~\cite{linuxkernel-bpf-verifier}, from the verifier log the kernel produced, below the source language where the lowering-artifact class originates~\cite{llvm-bpf-reloc, alden2024llvm}.

\noindent\textbf{Fault localization and proof explanation.} Techniques that pinpoint or explain a failure predate eBPF, but each needs an input \sys does not have. Spectrum-based fault localization ranks suspect statements by how their execution correlates with test failures, needing many runs and a test suite~\cite{wong2016survey, jones2005empirical}; program slicing extracts the statements a value depends on, needing the program's dependence graph~\cite{weiser1981program}; and unsat-core extraction names a minimal infeasible subset of constraints from a solver's structured search artifacts~\cite{cimatti2011computing}. \sys works from a single verifier rejection log, locating the fix site and explaining why the program was rejected. The kernel verifier simulates execution over an abstract state and emits no such artifact~\cite{linuxkernel-bpf-verifier, gershuni2019simple}.