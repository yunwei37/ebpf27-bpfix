\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/repair.pdf}
  \caption{Programs repaired on \testset (of 75) from the raw verifier log against the \sys diagnostic, at one attempt (left) and at two attempts (right), on the same scale. The second attempt is one failure-informed retry. \sys leads in every condition. Qwen2.5-3B is a one-attempt capacity-stress baseline, so it appears only in the one-attempt panel.}
  \label{fig:repair}
\end{figure}